const str = "A Script is a small piece of program that can add interactivity to your website"

const str1 = "Java is a popular programming language"

word = "Script";

console.log(`${word} word ${str.includes(word) ? 'is' : 'is not'} present in str`);

console.log(`${word} word ${str1.includes(word) ? 'is' : 'is not'} present in str1`);